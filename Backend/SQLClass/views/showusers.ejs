<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <style>
        table, th, td{
            border: 1px solid black;
        }
        button{
            background-color: rgb(0, 115, 255);
            color: white;
        }

        .delete{
            background-color: red;
            color: white;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>Email</th>
            <th>Username</th>
        </tr>

        <% for(user of users){ %>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.email %></td>
                <td><%= user.username %></td>
                <td>
                    <form method="GET" action="/user/<%= user.id %>/edit">
                        <button>Edit Username</button>
                    </form>
                </td>
                <td>
                    <form method="GET" action="/user/<%= user.id %>/delete">
                        <button class="delete">Delete User</button>
                    </form>
                </td>
            </tr>
        <% } %>
    </table>
    <br>
    <a href="http://localhost:8080/user/new"><button>New User</button></a>
</body>
</html>